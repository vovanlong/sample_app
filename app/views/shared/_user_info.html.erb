<%= link_to gravatar_for(current_user, size: Settings.user_info_html.number_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("shared.user_info_html.link_view"), current_user %></span>
<span><%= pluralize current_user.microposts.count, "micropost" %></span>
